# PathPlanA Sample Deploy

This folder keeps the old `pathplana` deploy package as sample/export content for the PathPlanA tool.

It is no longer used by the live robot deploy. The live autonomous deploy assets now come from:

- `src/main/deploy/pathplanner`
- `src/main/deploy/choreo`
